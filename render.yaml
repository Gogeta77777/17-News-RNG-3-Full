services:
  - type: web
    name: 17-news-rng-3
    env: node
    plan: starter
    branch: main
    buildCommand: npm ci --prefer-offline --no-audit
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
