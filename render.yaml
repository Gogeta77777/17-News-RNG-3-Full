services:
  - type: web
    name: 17-news-rng-3
    env: node
    plan: starter
    branch: main
    buildCommand: npm ci
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
